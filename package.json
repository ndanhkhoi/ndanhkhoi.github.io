{
  "name": "ndanhkhoi.github.io",
  "private": true,
  "version": "1.0.0",
  "description": "Personal CV - print-first A4 HTML (Eleventy + Paged.js)",
  "scripts": {
    "dev": "npm run build && npm run build:pdf && eleventy --serve",
    "build": "eleventy",
    "build:pdf": "node scripts/build-pdf.mjs",
    "check:view": "python3 scripts/check_view.py"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0",
    "pagedjs": "^0.4.3",
    "puppeteer": "^24.0.0"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0"
  }
}
